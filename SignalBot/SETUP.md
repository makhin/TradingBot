# SignalBot Setup Guide

Это руководство поможет вам настроить SignalBot для автоматического исполнения торговых сигналов из Telegram.

## Предварительные требования

- .NET 8.0 SDK
- Binance Futures аккаунт (testnet для тестирования)
- Telegram аккаунт
- Доступ к Telegram каналам с торговыми сигналами

## Шаг 1: Получение Telegram API Credentials

SignalBot использует WTelegramClient для подключения к Telegram. Вам нужно получить API credentials:

### 1.1 Создание Telegram Application

1. Перейдите на https://my.telegram.org/apps
2. Войдите с вашим номером телефона
3. Нажмите "API development tools"
4. Заполните форму:
   - **App title**: SignalBot (или любое имя)
   - **Short name**: signalbot
   - **Platform**: Desktop
   - **Description**: Trading signal bot
5. Нажмите "Create application"
6. Сохраните:
   - **api_id** (число, например: 12345678)
   - **api_hash** (строка, например: 0123456789abcdef0123456789abcdef)

⚠️ **ВАЖНО**: Никогда не публикуйте эти credentials публично!

### 1.2 Получение Telegram Bot Token (для уведомлений, опционально)

Если хотите получать уведомления через Telegram бота:

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям (укажите имя и username)
4. Сохраните **Bot Token** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
6. Отправьте ему любое сообщение
7. Сохраните ваш **Chat ID** (число, например: `123456789`)

## Шаг 2: Получение Binance API Keys

### 2.1 Testnet (рекомендуется для начала)

1. Перейдите на https://testnet.binance.vision/
2. Зарегистрируйтесь через GitHub
3. Перейдите в API Key Management
4. Создайте новый API ключ
5. Сохраните **API Key** и **Secret Key**
6. ✅ Testnet использует виртуальные средства - безопасно для тестирования

### 2.2 Mainnet (только после тестирования!)

⚠️ **ВНИМАНИЕ**: Используйте mainnet только после тщательного тестирования на testnet!

1. Перейдите на https://www.binance.com
2. Войдите в аккаунт
3. Перейдите в [Profile] → [API Management]
4. Создайте новый API ключ
5. Включите **Enable Futures** permission
6. Настройте **IP Restrictions** (рекомендуется)
7. Сохраните **API Key** и **Secret Key**

## Шаг 3: Настройка .env файла

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cd SignalBot
   cp .env.example .env
   ```

2. Откройте `.env` и заполните ваши credentials:

```bash
# Telegram API (обязательно)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abc123def456...
TELEGRAM_PHONE=+79001234567

# Telegram Bot (опционально, для уведомлений)
TELEGRAM_BOT_TOKEN=123456789:ABC...
TELEGRAM_CHAT_ID=123456789

# Binance Testnet (для тестирования)
BINANCE_TESTNET_KEY=your_testnet_key
BINANCE_TESTNET_SECRET=your_testnet_secret

# Binance Mainnet (для реальной торговли)
BINANCE_MAINNET_KEY=your_mainnet_key
BINANCE_MAINNET_SECRET=your_mainnet_secret

# Режим (true = testnet, false = mainnet)
TRADING_BinanceApi__UseTestnet=true
```

3. Сохраните файл

⚠️ **БЕЗОПАСНОСТЬ**: Убедитесь, что `.env` добавлен в `.gitignore`!

## Шаг 4: Настройка appsettings.json

Откройте `appsettings.json` и настройте параметры бота:

### 4.1 Telegram Channels

Добавьте ID каналов, которые бот будет мониторить:

```json
"Telegram": {
  "ChannelIds": [
    -1001234567890,
    -1009876543210
  ]
}
```

**Как получить Channel ID:**

1. Запустите SignalBot один раз (он покажет все ваши каналы)
2. Или используйте [@username_to_id_bot](https://t.me/username_to_id_bot)
3. Отправьте боту ссылку на канал (например: `https://t.me/yourchannel`)
4. Бот вернет Channel ID (отрицательное число)

### 4.2 Risk Management

Настройте риск-менеджмент:

```json
"RiskOverride": {
  "MaxLeverage": 10,
  "RiskPerTradePercent": 1.0,
  "MaxDrawdownPercent": 20.0,
  "MaxDailyLossPercent": 5.0
}
```

### 4.3 Trading Settings

```json
"Trading": {
  "MaxConcurrentPositions": 5,
  "MinTimeBetweenSignals": "00:01:00"
}
```

## Шаг 5: Первый запуск

### 5.1 Build проекта

```bash
cd SignalBot
dotnet build
```

### 5.2 Запуск

```bash
dotnet run
```

### 5.3 Аутентификация в Telegram

При первом запуске WTelegramClient запросит:

1. **Phone number**: Ваш номер телефона (формат: +79001234567)
2. **Verification code**: Код из Telegram сообщения
3. **2FA Password**: Если у вас включена двухфакторная аутентификация

После успешной аутентификации создастся файл сессии `telegram_session.dat`.

⚠️ **БЕЗОПАСНОСТЬ**: Файл сессии содержит авторизационные данные - храните его в безопасности!

### 5.4 Проверка работы

Бот должен вывести:

```
[INFO] SignalBot starting up...
[INFO] Using Binance Testnet API
[INFO] Logged in as YourName (@12345678)
[INFO] Found 25 dialogs
[INFO] Monitoring channel: Trading Signals (ID: -1001234567890)
[INFO] Order monitor started successfully
[INFO] SignalBot is running. Press Ctrl+C to stop.
```

## Шаг 6: Тестирование

### 6.1 Проверка парсинга сигналов

Отправьте тестовый сигнал в канал (или дождитесь реального):

```
#BTC/USDT - Long🟢

Entry: 50000
Stop Loss: 49000

Target 1: 50500
Target 2: 51000
Target 3: 51500

Leverage: x5
```

Бот должен:
1. Распознать сигнал
2. Валидировать параметры
3. Открыть позицию на Binance
4. Разместить SL и TP ордера
5. Отправить уведомление (если настроено)

### 6.2 Мониторинг логов

Логи сохраняются в:
- Консоль (real-time)
- `logs/signalbot-YYYYMMDD.txt`

Проверьте логи на наличие ошибок:

```bash
tail -f logs/signalbot-*.txt
```

### 6.3 Проверка позиций на Binance

Зайдите на https://testnet.binance.vision/ и проверьте:
- Открыта ли позиция
- Установлены ли SL/TP ордера
- Правильно ли рассчитан размер позиции

## Шаг 7: Переход на Mainnet (опционально)

⚠️ **ВНИМАНИЕ**: Переходите на mainnet только после:
- ✅ Тестирования на testnet минимум 1-2 недели
- ✅ Проверки всех edge cases
- ✅ Убедитесь, что риск-менеджмент работает корректно
- ✅ Начните с минимального капитала

### 7.1 Изменить .env

```bash
TRADING_BinanceApi__UseTestnet=false
```

### 7.2 Перезапустить бота

```bash
dotnet run
```

Бот должен вывести:

```
[INFO] Using Binance Mainnet API
```

## Troubleshooting

### Ошибка: "Failed to login to Telegram"

- Проверьте TELEGRAM_API_ID и TELEGRAM_API_HASH
- Убедитесь, что phone number в международном формате (+79001234567)
- Проверьте интернет-соединение

### Ошибка: "Binance API error: Invalid API-key"

- Проверьте BINANCE_TESTNET_KEY/BINANCE_MAINNET_KEY
- Убедитесь, что ключ активен на Binance
- Для mainnet: проверьте IP restrictions

### Бот не видит каналы

- Убедитесь, что вы подписаны на канал
- Проверьте, что канал публичный или вы имеете доступ
- Channel ID должен быть отрицательным числом

### Сигналы не распознаются

- Проверьте формат сигнала (см. README.md)
- Посмотрите логи: `[DEBUG] Failed to parse signal: ...`
- Убедитесь, что сигнал содержит все обязательные поля

## Безопасность

### ✅ Обязательно:

1. **Никогда** не коммитьте `.env` в Git
2. Используйте **IP restrictions** для Binance API keys
3. Храните **session файл** в безопасности
4. Начинайте с **testnet**
5. Используйте **небольшие позиции** на mainnet
6. Настройте **MaxDrawdownPercent** и **MaxDailyLossPercent**

### ⚠️ Риски:

- Торговля на futures высокорискованна
- Бот может открывать позиции автоматически
- Убедитесь, что сигналы из доверенного источника
- Всегда мониторьте работу бота
- Используйте stop-loss

## Поддержка

- GitHub Issues: https://github.com/yourusername/TradingBot/issues
- Документация: [docs/SIGNALBOT_DESIGN.md](../docs/SIGNALBOT_DESIGN.md)
- README: [SignalBot/README.md](README.md)

## Лицензия

См. [LICENSE](../LICENSE)
